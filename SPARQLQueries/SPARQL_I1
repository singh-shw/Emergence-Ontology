#I1 - Which authors have provided definitions for more than one emergent behavior?

SELECT ?author (COUNT(DISTINCT ?behavior) AS ?behaviorCount)
WHERE {
    ?author rdf:type BASE:Author .
    ?author BASE:defines ?behavior .
    ?behavior rdf:type BASE:EmergentBehavior .
} 
GROUP BY ?author
HAVING (?behaviorCount > 1)
