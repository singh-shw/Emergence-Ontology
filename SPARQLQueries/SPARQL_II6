# Which authors have discussed [EmergentBehavior]?

SELECT DISTINCT ?authorName
WHERE {
    ?author rdf:type BASE:Author .
    ?author BASE:hasName ?authorName .
    ?author BASE:defines ?behavior .
    ?behavior rdf:type ?behaviorClass .
    ?behaviorClass owl:equivalentClass {Class:subClassOf(EmergentBehavior)}
}
